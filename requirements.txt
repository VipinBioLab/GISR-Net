# GISR-Net requirements
#
# For a CUDA build of PyTorch, install torch/torchvision first from
# https://pytorch.org/get-started/locally/ and then run this file.

torch>=1.13
torchvision>=0.14
numpy>=1.21
scipy>=1.7
matplotlib>=3.5
pandas>=1.3
Pillow>=9.0

# only needed by the scripts in docs/ that build the Word documents
python-docx>=0.8.11
